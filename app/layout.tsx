import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'SEO Content Optimizer - Analyze, Optimize, and Export SEO-Ready Content',
  description: 'Professional SEO Content Improvement Tool that crawls web pages, analyzes their SEO elements, and generates comprehensive Word document reports with optimized recommendations.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
